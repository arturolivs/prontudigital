# ============================================================
# Variáveis de ambiente — Desenvolvimento
# Copie para .env e ajuste os valores
# NUNCA commite o .env real no git
# ============================================================

# Banco de dados
POSTGRES_DB=prontudigital
POSTGRES_USER=user_admin
POSTGRES_PASSWORD=secret123

# JWT — desenvolvimento (valores fixos para facilitar debug)
JWT_ACCESS_EXPIRATION_MS=600000000
JWT_REFRESH_EXPIRATION_MS=600000000
JWT_SECRET=dev-secret-key-minimo-32-caracteres-aqui

# Portas expostas no host (útil para acessar direto sem gateway)
BACKEND_PORT=8080
GATEWAY_PORT=9090
FRONTEND_PORT=3000
POSTGRES_PORT=5432
