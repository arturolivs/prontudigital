package com.prontudigital.backend.prontuario.enums;

/** Tipos de atestado emitidos pela clinica (RF17). */
public enum TipoAtestado {

    /** Declara que o paciente esteve na clinica; nao afasta de atividades. */
    COMPARECIMENTO,

    /** Recomenda afastamento por um numero de dias. */
    AFASTAMENTO
}
